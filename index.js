function klik()
{
	window.open('login.php'); //This will open Login.html in a new window.
}
function register()
{
	window.open('Register.php'); //This will open HTML(1) in a new window.
}
function logout()
{
	window.open('logout.php'); //This will open HTML(1) in a new window.
}